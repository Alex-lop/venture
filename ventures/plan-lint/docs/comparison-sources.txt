# Every page docs/comparison.md cites, and the day it was read.
# scripts/refresh-comparison.sh re-fetches this list and re-checks every
# quotation on that page against what comes back. tests/test_comparison_truth.py
# fails when the page cites a URL that is not listed here.
#
# fetched: 2026-08-31 UTC
# local date at that moment: 2026-08-30
#
# Below the URLs: every figure about another project that docs/comparison.md
# states, with the command that read it on the date above.
# tests/test_comparison_truth.py fails when the page states a figure that is not
# recorded here, and scripts/refresh-comparison.sh re-reads every one of them.
#
# gh api repos/cirbuk/plan-lint -q .pushed_at      -> 2025-08-09T07:19:53Z
figure: last pushed 2025-08-09
# curl https://pypi.org/pypi/plan-lint/json        -> 0.0.3, 2025-04-29T06:58:45Z
figure: last released 2025-04-29 (v0.0.3)
# python -c "import zipfile,urllib.request; ..." over the published
# plan_lint-0.0.3-py3-none-any.whl entry_points.txt -> plan-lint = plan_lint.cli:app
figure: the `plan-lint` console script
#
# Star counts, each read with `gh api repos/<slug> -q .stargazers_count` on the
# date above. scripts/refresh-comparison.sh re-reads them live and fails when
# one has drifted past the tolerance the page states.
figure: `open-policy-agent/opa` 12,179
figure: `cedar-policy/cedar` 1,701
figure: `kyverno/kyverno` 8,088
figure: `open-policy-agent/conftest` 3,256
figure: `open-policy-agent/regal` 402
figure: `langchain-ai/langgraph` 40,733
figure: `crewAIInc/crewAI` 57,841
figure: `microsoft/agentrc` 1,036
figure: `cirbuk/plan-lint` 13
#
# Provenance of the port. README.md calls Graphene Apache-2.0 and
# docs/porting-notes.md names the five source paths that were ported; both are
# claims about another repository, so both are read and recorded here on the
# date above. tests/test_comparison_truth.py holds the two pages to these lines.
# head -2 LICENSE                     -> "Apache License" / "Version 2.0, January 2004"
provenance: Graphene LICENSE Apache-2.0
# test -f backend/graphene/orchestration/validation.py      -> present
provenance: ported path `backend/graphene/orchestration/validation.py`
# test -f backend/graphene/orchestration/mission_models.py  -> present
provenance: ported path `orchestration/mission_models.py`
# test -f backend/graphene/core_models.py                   -> present
provenance: ported path `core_models.py`
# test -f backend/graphene/hashing.py                       -> present
provenance: ported path `hashing.py`
# test -f tests/unit/orchestration/test_validation.py       -> present
provenance: ported path `tests/unit/orchestration/test_validation.py`
#
# One line per body row of every comparison table: the tool the row is about, and
# the page that row cites. A row is a public claim about someone else's project,
# so a reviewer has to add it here by hand before it can appear on the page --
# which is what a wholly fabricated row, citing a URL already in this manifest,
# walked straight through. tests/test_comparison_truth.py holds the two together
# in both directions.
row: Open Policy Agent / Rego https://www.openpolicyagent.org/docs/policy-language
row: Cedar https://docs.cedarpolicy.com/
row: Kyverno https://kyverno.io/docs/introduction/
row: conftest https://github.com/open-policy-agent/conftest
row: Regal https://github.com/open-policy-agent/regal
row: LangGraph https://docs.langchain.com/oss/python/langgraph/graph-api
row: CrewAI planning https://docs.crewai.com/en/concepts/planning
row: Claude Code hooks https://code.claude.com/docs/en/hooks
row: Cursor rules https://cursor.com/docs/context/rules
row: microsoft/agentrc https://github.com/microsoft/agentrc
#
https://code.claude.com/docs/en/hooks
https://cursor.com/docs/context/rules
https://docs.cedarpolicy.com/
https://docs.crewai.com/en/concepts/planning
https://docs.langchain.com/oss/python/langgraph/graph-api
https://github.com/cirbuk/plan-lint
https://github.com/microsoft/agentrc
https://github.com/open-policy-agent/conftest
https://github.com/open-policy-agent/regal
https://kyverno.io/docs/introduction/
https://www.openpolicyagent.org/docs/policy-language
